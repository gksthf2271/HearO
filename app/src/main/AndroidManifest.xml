<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.junseo.test03">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hearobutton"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistActivity" />
        <activity
            android:name=".MenuActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".DeviceListActivity"  android:theme="@android:style/Theme.Dialog" android:configChanges="keyboardHidden|orientation|screenSize"/>
        <activity android:name=".FindpasswordActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".VersionActivity" />
        <activity android:name=".AlarmActivity" />
        <activity
            android:name=".MacroActivity"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".STTActivity" />

        <activity
            android:name=".arduino.BluetoothPairActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".StartActivity" />
        <service android:name=".service.BTCTemplateService"></service>



    </application>

</manifest>